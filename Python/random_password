import random
import string

def get_random_string(length):
    letters = string.ascii_letters
    res_pass = ''.join(random.choice(letters) for i in range(length))
    print("Your random password is:", res_pass)

get_random_string(8)
